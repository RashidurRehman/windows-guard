{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "badge",
  "description": "Capability for per-app defender status badge windows",
  "windows": ["guard-badge-*"],
  "permissions": ["core:event:default", "core:default"]
}
