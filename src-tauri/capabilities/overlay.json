{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the Sync Monitor blink-alert overlay window",
  "windows": ["sync-overlay"],
  "permissions": ["core:event:default"]
}
